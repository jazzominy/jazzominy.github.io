{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run jekyll",
            "type": "shell",
            "command": "source ~/.zshrc && bundle exec jekyll serve --livereload --drafts",
            "problemMatcher": [],
            "windows": {
                "command": "bundle exec jekyll serve --livereload --drafts"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
